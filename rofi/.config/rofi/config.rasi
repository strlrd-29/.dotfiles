/* Dark theme. */
/* @import "~/.cache/wal/colors-rofi-dark" */

/* Light theme. */
@import "~/.cache/wal/colors-rofi-light"

configuration {
  font: "JetBrains Mono 16";
  terminal: "alacritty";
  timeout {
      action: "kb-cancel";
      delay:  0;
  }
}
